# `cli/`

## 目录职责

这里存放用户可见的 `xiao` 命令行和终端交互层。CLI 负责参数、配置、Shell、提示符、进程边界和显示，不执行 Xiao 指令。

## 工程期

11 建立核心命令；11A 管理虚拟环境和包；11B 实现 REPL；11C 接入国际化；18–19 接入优化产物和发布流程。

## 子目录

- `ts/`：TypeScript workspace，见其 README。

