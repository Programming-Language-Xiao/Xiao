# `xiao-optimizer`

## 目录职责

实现共享 IR 与后端降低前的优化 Pass、效果分析、验证、优化指纹和失败回退接口。所有变换必须保持 Xiao 的溢出、随机、I/O、错误和 `drop` 语义。

## 工程期

13 建立统一管线；14 接入字节码优化；15 接入 LLVM 原生优化。

## 模块放置

Pass 注册、效果/别名分析、验证、配置指纹和报告放在 `src/`；优化级别命令行解析放在 CLI。

## 禁止事项

不解析 CLI 的 `-O` 参数、不写入不可变缓存、不用未定义行为假设替代语言规则。
