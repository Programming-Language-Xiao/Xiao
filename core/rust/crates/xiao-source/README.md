# `xiao-source`

## 目录职责

提供 UTF-8 源码缓冲区、游标、字节偏移、行列映射、源码区间和文件身份。所有前端诊断与源码映射依赖这里的稳定类型。

## 工程期

01（源码位置与最小 Token），后续所有前端、字节码和原生诊断阶段复用。

## 模块放置

源码缓冲区、游标、行列索引和 span 相关模块放在 `src/`；编辑器缓冲区和终端光标不放在这里。

## 允许依赖与禁止事项

只依赖基础库和必要的 Unicode 支持；不放 Token 规则、解析器、终端输出或平台路径拼接。公共位置类型必须文档齐全并保持版本稳定。
