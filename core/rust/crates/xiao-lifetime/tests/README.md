# `xiao-lifetime/tests`

## 工程期

06-A（静态生命周期闭环）。

## 职责与边界

`a06_lifetime.rs` 是 06-A 的规格级回归入口，覆盖单作用域、嵌套作用域、分支
合流、循环控制、函数边界、返回转移、容器拥有、闭包捕获、自引用/多节点强环、
弱边、拓扑释放、表达式临时值和动态值保守提升。测试只验证静态结果，不启动
Runtime 或执行 Xiao 程序。
