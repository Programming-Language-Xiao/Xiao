# `xiao-vm/src/research/machine`

放置三地址解释器各候选机型的载体实现，对应工程期 09R2。每个文件是一种机型，
只实现 `carrier.rs` 的接口，不含任何语言语义。

当前只有 `stack.rs`（栈式载体）。批次 2 增加分类型寄存器机型与混合式机型时
应当只在这里新增文件，`../semantics/` 一行不改。禁止把机型条件分支写进语义核。
