# `xiao-vm/src/research/semantics`

放置机型无关的三地址语义核，对应工程期 09R2。它只认识三地址指令和
`../carrier.rs` 的窄接口，**不认识「栈」这个词**——这是批次 2 能否只加载体文件
而不改语义核的判据。

禁止在这里出现机型条件分支、载体专属词汇或逐指令观测回调；值运算一律经
`../ops.rs` 收口，不散落裸 `RuntimeValue` 调用。
