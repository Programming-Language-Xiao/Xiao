# `xiao-bytecode/src/encode`

工程期 09；这里实现 09R3 冻结的内存字节码编码、解码和输入验证，布局版本为 `3`，opcode
范围为 `0..40`。实现只处理内存中的 TAC，不生成正式 `.xiaoc` 文件；正式容器归第 14 阶段。

职责：按冻结字段顺序编解码、建立 `pc -> IrSpan` 映射，并拒绝坏版本、坏引用、未知 opcode、
截断和定宽溢出。类型和生命周期事实由前端 IR/TAC 提供，不在编码器中重新推断。
