# `xiao-types`

## 目录职责

提供静态类型、动态值边界、类型推断、显式转换、固定宽度数值、溢出检查、容器元素约束和布尔加减规则的编译期表示。

## 工程期

02–04 建立基础类型；08 接入统一前端和 IR；后续由两个后端共同消费。

## 模块放置

标量/容器类型、推断、转换、数值溢出和选择器类型规则放在 `src/`；后端布局类型放在各后端 crate。

## 禁止事项

不编码 LLVM 指令、不执行用户代码、不把平台 C 类型宽度当作 Xiao 类型定义。
