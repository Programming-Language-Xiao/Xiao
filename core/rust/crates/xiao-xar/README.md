# `xiao-xar`

## 目录职责

实现 ZIP/ZIP64 兼容的 `.xar` 打包、索引成员、入口定位、依赖闭包、资源和归档安全验证。

## 工程期

17；18 由 CLI 提供 `build -xar` 与 `-xar` 路由；19 做发布和损坏恢复验收。

## 模块放置

ZIP 读写、归档清单、入口解析、成员安全和验证放在 `src/`；平台文件关联放在 `xiao-platform`/CLI。

## 禁止事项

不执行未验证模块，不携带官方 Runtime 依赖，不绕过路径穿越、重复成员、摘要或版本检查。
