# `xiao-artifacts`

## 目录职责

实现 SHA-256 内容寻址对象、不可变缓存、对象完整性校验、原子写入和 Protobuf 索引。缓存对象与依赖锁、`.xar` 清单保持职责分离。

## 工程期

16；14/17/18 提供字节码、归档和 CLI 接入。

## 模块放置

对象存储、SHA-256、原子事务、缓存淘汰和 Protobuf 索引放在 `src/`；归档索引组合由 `xiao-xar` 完成。

## 约束

摘要针对最终规范对象的完整字节；读取时必须重算校验；不能原地改写公开 `.xiaoc`。
