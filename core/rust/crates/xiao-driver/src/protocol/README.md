# `protocol/`

这里是 `xiao-driver` 的协议实现子模块。`protocol.rs` 只保留稳定门面和公开重导出，
子模块按职责形成单向依赖：帧、消息和请求是叶子；映射与校验位于中层；运行、构建和
配置编排位于上层；服务分发最后依赖前面各层。

当前已落地 `frame.rs`：它只负责 8 字节大端长度前缀、16 MiB 上限、JSON 编解码和
帧级稳定错误。它不启动线程、不访问文件系统，也不依赖服务分发。后续模块必须沿同一
方向下沉共享逻辑，不能让叶子反向依赖门面或服务层。
