# `xiao-modules`

## 目录职责

按 `.xiao` 文件建立模块身份、目录命名空间、导入图、包外导出和确定性初始化顺序，并连接配置声明与源码导入。

## 工程期

05、08；11A 接入锁定外部包图和环境视图。

## 模块放置

模块身份、目录命名空间、导入图、导出表和初始化排序放在 `src/`；包源下载和环境物化放在 `xiao-package`。

## 禁止事项

不下载包、不修改 Shell 状态、不执行目录初始化代码；模块至多初始化一次的规则由 Runtime/VM 实现。
