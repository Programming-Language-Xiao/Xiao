# 12. 测试与开发里程碑

> 本阶段不是最后才做的测试收尾，而是每个前置阶段的退出门槛。没有双模式一致性测试，就不能宣称语言规划已经落地。

退出条件分为两类：当前阶段可以直接执行的单元/规格门槛，以及必须等待后置消费者出现的集成契约。前者未通过时不能进入下一实施阶段；后者必须登记并在对应的字节码、LLVM、CLI 或跨平台里程碑补跑，但不能形成“先完成后端，才能开始建设后端所需前端”的循环依赖。整个工具链宣称完成前，两类条件都必须通过。

## 一级工程目标：建立测试分层

### 单元测试

覆盖词法 Token、缩进、反引号标识符、路径与选择器解析、类型推断、数值运算和引用计数基础操作。单元测试应尽量不依赖操作系统。

### 架构与耦合测试

每个阶段都必须检查模块依赖方向、禁止循环依赖、公共接口边界和目录 README 登记；
语义重复实现或跨层私有访问应在架构检查阶段失败，而不是等集成测试发现。该检查与
代码、规格测试和 UseDocs 同步执行。

### 规格测试

每条已确定语法至少有：

- 一个最小合法例子。
- 一个最小非法例子。
- 一个包含错误位置和错误编号的期望结果。

### 双模式一致性测试

同一源文件分别经过字节码和 LLVM 后端，比较标准输出、退出码、错误类别、容器顺序和 `drop` 记录。允许机器码优化改变性能，不允许改变语义。

### 跨平台测试

按 **Windows → Linux → macOS** 的顺序建立平台门槛，逐个平台验证换行、路径、固定宽度数值、环境注入、原生链接、退出码和交互终端行为；后一个平台接入后仍必须重跑前一个平台的回归。涉及按键、粘贴、软换行与颜色的用例必须使用可控的伪终端或等价终端测试层，不能只测试普通标准输入管道。

### 实现语言边界测试

执行核心的构建与测试必须证明字节码 VM 和执行时 Xiao Runtime 来自 Rust 实现；TypeScript 只覆盖 CLI/REPL 的输入、配置、进程和显示边界。测试应通过结构化请求/结果验证两层接口，而不是把本地化文本或 Rust 内部布局当作契约。编译器前端、共享优化器和 LLVM 后端的实现语言若尚未单独确认，不得在本层测试中擅自扩大为 Rust 强制要求。

## 一级工程目标：按真实规模拆分里程碑

这些里程碑表示依赖关系，不表示会话数或固定发布日期。每个里程碑都可能继续拆成很多设计、实现、测试和修复提交。尚未轮到的阶段只维护规范，不提前塞进当前实现。

### A0：工程骨架与文档门槛

实现细则以 [A0. 工作区与质量门禁实现方案](00a-a0-workspace-and-checkers.md) 为准；A0 至少拆成 A0.1 manifest 与骨架、A0.2 目录检查器、A0.3 覆盖率检查器、A0.4 CI 与交接四个二级任务。A0 不实现任何 Xiao 语言语义。

退出条件：

1. Rust 核心、TypeScript CLI、平台适配、测试、工具和资源目录按 [00A. 工程框架与目录布局](00a-project-layout.md) 创建；每个代码目录都有同级 README，能标识内容、工程期和模块归属。
2. `Cargo.toml`、TypeScript workspace manifest、仓库政策清单和实际文件系统逐项一致；不使用未登记的宽泛 workspace glob。
3. 目录检查器能报告缺失/多余 README、路径越界、符号链接逃逸、大小写冲突、重复包名和未登记源目录，并输出稳定错误码及 JSON/SARIF。
4. 依赖方向和 Rust/TypeScript 边界通过目录检查；CLI 不包含第二套 Xiao 语义实现。
5. 文档覆盖率检查器能统计 Rust/TypeScript/测试辅助代码，公共 API 文档达到 100%，全仓库函数/方法/类/模块文档达到 90% 以上；解析失败不得假通过。
6. `docs/UseDocs` 具备根索引、主题索引和至少一层子主题；模块登记能关联代码、测试与 UseDocs，已完成模块没有缺失或非 `verified` 页面。
7. 代码、测试和 UseDocs 的同步规则已接入本地检查与 CI；模块未同步文档时不能标记完成。
8. 平台适配计划明确按 Windows → Linux → macOS 排列；09R3 使用同机 Rust 栈式原型基线，
   LLVM 原生性能验收留到 10/15 阶段。
9. 20 个 Rust workspace 成员均声明 `[lints] workspace = true`；`missing_docs` 的 rustc 与
   repo-check 两套口径均无缺口，标准 Clippy 门禁能阻断新增缺失 Rustdoc（实现提交
   `6d296c3`）。
10. `A0-SIZE-001` 对项目维护源码执行 2500 物理行门禁，超标时在 JSON/SARIF 与文本报告中
    提供结构化树形大纲；解析大纲失败不得取消尺寸 `error`，四段齐全的旁置硬耦合说明才可
    降级为可见 `warning`。研究编码器的 `encode.rs` 与语法解析器的 `parser.rs` 均已拆为
    门面和职责子模块；当前门禁不应保留尺寸债务，未来超标文件必须优先拆分而不是加豁免。

### F0：源码工程与位置模型

当前状态：已完成首批实现。`xiao-source` 已提供 UTF-8 校验、原始字节区间、Unicode 标量列号、
LF/CRLF 行索引和安全游标；详细交接见 [01A](01a-f0-l0-implementation.md)。

退出条件：

1. 编译器工程能够读取空文件和 UTF-8 源文件。
2. 源码游标稳定记录字节偏移、行号和列号。
3. `LF`、`CRLF` 和文件末尾行为都有单元测试。

### L0：最小 Token 流

当前状态：已完成首批实现。规格快照位于 `tests/spec/01-lexical`，由 `xiao-syntax` 集成测试
自动读取；非法字符使用 `X01-LEX-001` 并继续扫描。

退出条件：

1. 只需识别 ASCII 标识符、十进制整数、`=`、换行和文件结束。
2. `a = 1` 的 Token 种类、顺序和源码区间具有快照测试。
3. 非法字符能产生带位置的诊断，词法器不会死循环或崩溃。
4. 不要求 AST、类型检查、运行代码或集合支持。

### L1：基础词法扩展

当前状态：已完成首批实现。L1 快照位于 `tests/spec/01-lexical/l1-*.json`，由
`xiao-syntax` 集成测试自动读取；字符串、数字结构错误和最长运算符匹配均有独立正反例。

退出条件：

1. 字符串、浮点、布尔值、关键字和运算符按独立增量加入。
2. 括号、数组路径和表头符号能稳定分词。
3. `+` 只产生统一 Token，不在词法阶段实现集合并集。
4. `~`、`?`、`!`、逗号和比较符号能组成后续选择器所需的稳定 Token 序列，但词法阶段不判断索引语义。

### L2：UTF-8、注释与缩进

当前状态：已完成首批实现。L2 快照位于 `tests/spec/01-lexical/l2-*.json`，覆盖反引号名称、
文档注释、物理换行、缩进状态机、分隔符深度和错误恢复；实现交接见 [01C](01c-l2-implementation.md)。

退出条件：

1. 反引号 UTF-8 标识符和未闭合诊断可用。
2. 单行注释、文档注释、空行和缩进交互有规格测试。
3. `INDENT`、`DEDENT`、换行和文件结束序列稳定。

### P0：最小语法树

当前状态：已完成严格最小闭环。实现交接见 [01D P0 最小解析器与 AST](01d-p0-parser-implementation.md)，
正反快照位于 `tests/spec/02-parser`，集成入口为
`core/rust/crates/xiao-syntax/tests/parser_snapshots.rs`。

退出条件：

1. 只解析字面量、名称、独立表达式语句和简单赋值；多条顶层语句组成程序根节点。
2. AST 节点复用词法阶段的原始 UTF-8 字节区间，程序区间覆盖完整源码。
3. 文档注释挂接到相邻后续语句，文件末尾孤立注释保留在根节点中。
4. 错误恢复同步到 `Newline`、`Dedent` 或 `Eof`，不会改变后续合法 Token 的含义。
5. 运算、调用、索引、容器、控制流、类型声明和缩进代码块均有明确拒绝诊断，不能以未实现语法误报为成功。

二级测试任务：

1. `P0-AST`：验证五类字面量、普通/反引号名称、节点区间和 EOF 结束。
2. `P0-STATEMENT`：验证多条顶层语句、普通/反引号简单赋值及文档注释挂接。
3. `P0-RECOVERY`：验证非法左值、缺少右值、复杂尾部、`Indent`/`Dedent` 和错误后的后续语句。
4. `P0-SNAPSHOT`：维护 `tests/spec/02-parser` JSON 快照，并继续运行全部 `01-lexical` 快照。

### P1：表达式核心与索引选择器（已完成首批）

P1 只推进语法 AST，不把控制流、`def`、表头、入口声明或执行器提前带入。
具体交接契约见 [01E. P1 表达式与选择器实现](01e-p1-expression-selectors.md)。

二级实现任务：

1. `P1-AST`：在不改变 P0 节点形状的前提下加入一元/二元、调用、成员、`new`、`as`、选择器和扩展赋值节点。
2. `P1-PRATT`：实现固定优先级、结合性、软换行、调用参数和语句级赋值解析。
3. `P1-SELECTOR`：实现路径段、精确/范围/边界/全选/步长/随机选择和选择器错误恢复。
4. `P1-SNAPSHOT`：维护表达式与选择器正反快照，继续运行 `01-lexical` 和 `02-parser` 全部回归。

退出条件：

1. 所有已冻结表达式和选择器语法均有最小正例、负例、源码区间和稳定诊断测试。
2. 重复/重叠选择按书写顺序保留，混合选择项、负索引和 Unicode 码点索引均能形成 AST。
3. P1 不执行类型、边界、随机、容器写入或函数调用语义；未实现内容不能被误报为成功执行。
4. Rust 格式、测试、Clippy、Rustdoc、文档覆盖率和 UseDocs 同步门禁全部通过。

### S0：名称与静态标量类型

当前状态：已完成首批（实现与交接见 [02A](02a-p2-static-types.md)）；容器相关的 C0
已完成，交接边界见 [03A](03a-c0-containers.md)。

退出条件：

1. 名称解析、作用域和基础类型槽可用。
2. 固定宽度数值、转换和溢出规则可检查。
3. 推断声明与 `int a = 1` 等显式声明都能建立锁定的静态类型槽。
4. 普通变量可接受同类型重赋值，不兼容换型在运行前被拒绝。
5. `bool +/- 整数` 对偶数保持、对奇数翻转且结果仍为 `bool`；反向、布尔与布尔、布尔与浮点数运算均有负例。
6. 字符串类型只接受正式关键字 `str`；`string` 不被悄悄当成同义类型关键字。
7. `value as bool` 与 `bool(value)` 共享转换结果和错误类别；已确认的 `str` 字符串有正例，其他字符串和未获支持的源类型都有负例。
8. `bool` 转 `str` 的结果固定为小写 `"true"` 或 `"false"`，转换不会改变源变量的类型槽。

### C0：基础容器

退出条件：

1. C0-A 通过数组、元组、字典表、字典列 AST 和源码区间正反测试。
2. C0-B 通过结构化数组/元组/字典类型推导、显式数组元素约束、重复键拒绝和路径约束合并测试。
3. C0-C 通过 `list[0]`、`list[3/2]` 的精确读取、静态越界、键不存在、路径种类错误和未知边界测试。
4. 空数组只产生 `ContainerMaterializationPlan`，不得在类型阶段填充 Runtime 默认值；集合不属于本里程碑退出条件。
5. C0-D 已同步 `tests/spec/05-containers`、分层 UseDocs、目录 README 和模块登记，并通过文档/覆盖率门禁。
6. `const list[path]` 的运行时锁定提案、实际容器修改和范围/随机选择不得进入 C0。

### C1：有序容器选择器（已完成静态阶段）

实现交接见 [03B C1 交接记录](03b-c1-ordered-selectors.md)。本阶段的“完成”表示类型
检查器已经生成后端无关计划并通过静态门槛；真实容器读写、随机状态推进和运行时事务写入
仍由后续 Runtime/IR 阶段消费，不能在本阶段的 UseDocs 中宣称已经可执行。

已通过的退出条件：

1. 数组、元组、`str` 和字典列的同一对方括号多选、闭区间、前后范围与全量选择均能规范化。
2. 单个精确索引返回叶子类型；多选、范围和空结果按来源容器及嵌套路径投影，字典列重复
   直接键转为保序元组。
3. 正负非零步长方向稳定，零步长使用 `X03-TYPE-009`；静态字面量和整形表达式都有测试。
4. `[?x]` 与 `[!?x]` 分别记录无放回/放回计划；零数量、负数、超量和空来源边界都有
   `X03-TYPE-010`/`X03-TYPE-011` 诊断或确定空结果。
5. 字典表仍只允许单个精确键路径，集合仍完全不可索引；范围穿过无序字典表使用
   `X03-TYPE-008` 拒绝。
6. 负索引、Unicode 码点索引、重复/重叠项、父子路径合并、深度优先嵌套范围和每项步长
   都已有回归测试。
7. `random.seed` 计划、可注入 `RandomSource`、无放回/放回抽样边界和字典列重复键表示
   已冻结并有单元/集成测试。
8. 选择器左值已固定为事务性标量广播；容器右值、随机目标、复合赋值、复杂根和越界目标
   均有拒绝或不生成计划的回归测试。

定向验收命令：

```text
cargo test --manifest-path core/rust/Cargo.toml -p xiao-types --test c1_selectors
cargo test --manifest-path core/rust/Cargo.toml -p xiao-types --lib
cargo clippy --manifest-path core/rust/Cargo.toml -p xiao-types --all-targets -- -D warnings
```

### C2：集合与可哈希规则

退出条件：

1. 单一标量集合先完成唯一性、成员判断和不可索引约束。
2. 可哈希类型规则和动态插入检查可用。
3. 在 C2-B 静态闭环之后，再由独立子阶段实现 `+`、`&`、`-`、`^` 和集合比较。
4. `frozenset` 不作为本里程碑的强制退出条件。
5. 在实现不同显式元素类型之间的集合运算前，冻结结果元素类型或拒绝规则，并补齐正反例。

#### C2-A：最小集合静态闭环（已完成静态阶段）

1. 语法层能区分非空集合、空字典表和 `set()` 空集合构造式，并保留无序语义。
2. 类型层能推断/检查单一元素类型，允许 `none`，将 `bool` 与数值严格分离。
3. 静态重复元素、不可哈希元素、显式类型冲突和集合索引行为都有稳定诊断。
4. `in`/`not in` 对集合形成 `bool` 结果；集合代数、Runtime 和 `frozenset` 明确不在范围内。
5. Rustdoc、规格快照、UseDocs、模块登记和国际化字段（`code`、`message_id`、参数）
   在同一变更集中完成。

#### C2-B：异构集合与动态成员静态闭环（已完成静态阶段）

本子阶段只消费 AST 并生成静态类型与旁路 Runtime 检查标记，不创建集合对象。二级
SOP 与交接上下文见 [03D](03d-c2b-heterogeneous-sets.md)。

1. `set<T>`/`set<T | U>` 只允许变量根声明，类型项当前限于标量和 `none`；显式声明
   保留完整成员并集，`set()` 在声明上下文具体化。
2. 未显式声明的非空集合推导静态成员类型并集；旧式 `int name = {...}` 仍是同构
   约束，不能接受不兼容成员。
3. 静态成员按类型隔离相等与哈希身份；`bool` 不与整数/浮点合并，不同整数宽度
   也不合并。重复类型项在类型层去重，静态重复值仍产生稳定诊断。
4. 动态成员保留已知并集并登记 `SetHashability`；动态集合赋值或动态成员判断登记
   `SetMembership`，不能把延后检查当作静态证明。
5. 显式成员冲突、非集合初始化器、集合路径、`const set<...>` 和集合索引都有稳定
   `code`、`message_id` 与结构化参数；集合代数、Runtime 存储、元组递归哈希和
   `frozenset` 明确不在本子阶段。

退出条件：

1. `xiao-syntax` 的集合类型注解、`Pipe` Token、多行布局和错误恢复都有正反测试，且
   `set < value` 比较表达式不会被误判为声明。
2. `xiao-types` 能稳定表示 `Unknown`、同构集合和“静态成员并集 + 动态尾标”，并在
   统一、赋值、嵌套路径和成员判断中保持相同规则。
3. C2-B 快照固定阶段 `03D`、成功/失败状态、诊断编号、`message_id` 和参数；不比较
   本地化展示文本，也不改写 C2-A 历史快照。
4. C2-B 的代码、测试、目录 README、DevDocs、UseDocs 和模块登记在同一变更集中完成，
   Rustdoc 公共 API 100%、全仓库声明项 90% 以上，并通过 Rust/Bun 全部质量门禁。

#### C2-C：集合运算静态闭环（已完成静态阶段）

本子阶段只验证集合代数和比较的前端静态契约，不创建 Runtime 集合或执行集合值运算。

1. `+`、`&`、`-`、`^` 和 `+=`、`-=`、`&=`、`^=` 拥有稳定 Token、AST、优先级和
   左值锁定类型检查；`|`/`|=` 不表示集合并集。
2. `+`/`^` 结果取已知成员并集，`-` 保留左侧成员，`&` 取静态交集；静态空交集用
   `SetType::Empty` 表示，不能和 `set()` 的 `Unknown` 混淆。
3. `==`、`!=`、`<`、`<=`、`>`、`>=` 对集合返回 `bool`，允许异构集合比较；动态边界
   分别登记 `SetOperation`、`SetComparison`，原地写回需要 `SetMembership`。
4. 集合/标量混用、显式目标冲突和动态形状边界拥有稳定 `code`、`message_id` 与参数；
   快照不比较本地化文本。
5. C2-C 代码、测试、快照、DevDocs、UseDocs、目录 README 和模块登记必须同一变更集中
   完成，并通过 Rust/Bun 全部质量门禁。

#### C2-D 及以后

C2-C 通过后，集合 Runtime 存储、哈希、增删、真实代数执行、跨后端 lowering、元组递归
哈希和 `frozenset` 继续按独立子里程碑推进；后续实现必须复用 C2-C 的结果类型和检查标记。

### F1：函数与控制流语义

F1 按 04-A 至 04-D 顺序验收。当前只完成静态 AST/类型闭环；Runtime、IR 和后端消费是明确的后置债项。

#### F1-A：函数语法与 AST（已完成）

1. `def`、缩进函数体、普通/位置专用/关键字专用/`*args`/`**kwargs` 参数和默认值都有正反解析测试。
2. 调用参数的位置、关键字、`*` 展开和 `**` 展开保留稳定 AST；嵌套块的 `Dedent` 不吞掉外层分支。
3. 函数、条件、循环、返回和循环控制节点都保留原始源码区间与文档注释挂接。

#### F1-B：签名与类型推断（已完成）

1. 函数签名预登记支持递归、前向引用和反引号名称；参数作用域遮蔽不能破坏外层函数绑定。
2. 调用参数匹配覆盖默认值、重复传参、位置/关键字限制、可变参数和展开参数；`*args` 位置实参按元素类型统一。
3. 函数体、返回值和调用点共同提供 HM 约束；收尾阶段无法推断时给出稳定诊断，不静默降为任意动态类型。

#### F1-C：控制流与入口（已完成）

1. `if`/`elif`/`else`/`while` 只接受 `bool`；动态条件登记运行时检查，整数和字符串不做隐式真值转换。
2. `for in` 验证已知容器或动态边界；`break`/`continue` 只能位于最近循环，函数边界重置循环深度。
3. `return` 路径统一返回类型；省略返回值的函数收尾为 `none`；脚本模式与 `[main]` 工程模式记录统一入口元数据。

#### F1-D：静态质量门禁与交接（已完成）

1. Rust 定向测试、workspace 格式/检查/测试/Clippy/文档构建全部通过，测试只断言稳定结构字段。
2. 代码、测试、目录 README、DevDocs、UseDocs、模块登记和国际化字段在同一变更集中同步；公共 API 文档 100%，
   全仓库声明项至少 90%。
3. 已登记后置债项：控制流 lowering、Runtime 调用栈和动态检查、逃逸/释放、字节码/LLVM 双模式一致性、跨文件入口。

F1 的完整输入、交付物、不负责事项和 Agent 交接清单见 [04. 函数与控制流](04-functions-and-control.md)。

### D0：本地文件模块与配置骨架（进行中）

#### D0-A：绝对导入语法（已完成静态阶段）

1. `import`/`from` 的多目标、多选择项、别名、反引号符号和嵌套代码块都有正反解析测试。
2. `SourceSpan`、文档注释、节点索引和可恢复诊断保持稳定；相对、通配、动态字符串、括号
   续行和尾逗号明确拒绝。
3. 语法实现只消费 Token，不访问文件系统、配置或 Runtime；交接见 `05a-import-syntax.md`。

#### D0-B：本地发现与依赖图（已完成静态阶段）

1. `.xiao` 文件能按项目根映射为模块，目录无需 `__init__.py` 即形成纯命名空间；根/嵌套
   `config.xiao`、点目录、符号链接和 `target`/`node_modules` 边界具有测试。
2. 源码导入能生成直接/限定使用依赖边、词法绑定和顶层再导出；目录本身不执行初始化代码。
3. 文件/命名空间及大小写冲突、缺失目标/符号、限定符误用和循环都有稳定结构化诊断；循环时
   初始化序列为空。
4. `d0_modules.rs`、`tests/spec/06-modules`、分层 UseDocs、目录 README 和模块登记已同步；
   交接见 `05b-local-module-resolution.md`。

#### D0-C：表语法与静态生命周期契约（已完成静态阶段）

1. `[Table]`/`[[Table]]`、表体成员白名单、文档注释、缩进恢复和节点索引通过语法正反测试。
2. `TableType`/`TableSignature`、字段纯初始化、成员可见性、`new` 参数和 `init`/`drop` 签名
   通过类型层正反测试；结果只描述静态契约，不执行生命周期。
3. 表作为模块顶层符号、方法体递归作用域和公开导出通过模块集成测试；`xiao-modules` 不依赖
   `xiao-types`，不读取配置或创建 Runtime 值。
4. 三个 crate 的 `c05_tables.rs`、分层 UseDocs、目录 README、DevDocs 和模块登记已同步；
   交接见 `05c-table-static-closure.md`。

#### D0-D：配置与包边界（已完成静态阶段）

1. 全小写 `config.xiao` 通过独立 `ConfigDocument` 解析，支持项目身份、包外导出和递归
   静态值；项目入口可强制 `[project].name/version`。
2. 配置中的函数、调用、控制流、导入、表达式、重复键/表头、未知顶层表和严格字段
   未知字段在读取阶段以 `X05-CONFIG-*` 诊断拒绝。
3. 源码 `.xiao` 的 `[main]` 仍由语法/入口阶段负责，配置解析不执行或接管它。
4. `xiao-config` 的单元/公开规格测试、crate README、分层 UseDocs、DevDocs 和模块登记
   已同步；交接见 `05d-config-static-closure.md`。
5. 外部包身份、依赖锁定、多源索引和环境物化仍留给 11A；不得把这些能力回写进 05-D。

### R0：内存与表对象闭环

#### R0-A：生命周期静态闭环（已完成）

1. `xiao-lifetime` 消费既有 AST/类型结果，建立程序、函数、分支、循环和表作用域及控制流图。
2. 局部绑定、容器/表持有、闭包捕获、返回转移和动态边界均有强/弱边或 Runtime 检查记录。
3. 强环拒绝；`A -> B` 的强边拓扑、同层声明逆序和八类退出计划通过规格测试；损坏图输入不 panic。
4. crate README、分层 UseDocs、06A 交接记录和模块登记已同步。该阶段不创建 Runtime 对象。

#### R0-B：Runtime 对象与表生命周期（已完成首版）

实现交接见 [06B](06b-runtime-objects-and-tables.md)。本子阶段拆为五个二级任务：

1. `R0-B.1`：不透明对象头、固定宽度值和安全 Strong/Weak 句柄。
2. `R0-B.2`：可插拔引用计数接口与单线程非原子首版，覆盖归零、升级和不变量错误。
3. `R0-B.3`：释放计划驱动器和 `finally -> drop -> catch/传播` 错误展开。
4. `R0-B.4`：由 `TableSignature` 驱动的单例/实例状态机、字段检查、`init` 回滚和 `drop` 错误。
5. `R0-B.5`：`str`、标量运算、严格 `bool ± 整数` 以及独立 Rust 集成测试。

已通过的退出条件：

1. 消费 R0-A 的值/边/释放计划，实现引用计数和 `Weak` 句柄，并通过生命周期执行测试。
2. 消费 D0-C 的表签名后，实现 `[Table]`、`[[Table]]`、`new`、`init`、`drop` 的运行时行为，
   并验证与静态契约一致。
3. 没有追踪式 GC 或隐式全堆扫描。
4. 清理错误进入 `suppressed` 且不覆盖主错误；剩余释放绑定继续处理。
5. crate 分层 README、Runtime UseDocs、DevDocs 和模块登记已同步；VM、LLVM、容器和并发后置边界明确记录。

### H0：错误模型与并发边界

### H0-A：统一错误模型与报告器（07-A）

前置上下文：先阅读 [07. 错误模型与并发安全边界](07-concurrency-and-errors.md) 的 07-A 小节、`xiao-diagnostics` crate README 和 [UseDocs 错误报告](../UseDocs/troubleshooting/errors-and-reports.md)。本子阶段只建设错误对象、统一堆栈和报告记录，不实现 `try`/`catch` 语法、日志文件、调试窗口或并发执行器。

二级任务：

1. 将 `Diagnostic` 兼容保留在 `xiao-diagnostics`，新增 `XiaoError`/`XiaoResult`、`FatalError`、稳定 `X06`/`X07` 错误码和原因链。
2. 建立含源码区间、字节码偏移、原生地址、内联深度和帧类别的 `StackFrame` 模型。
3. 建立与具体序列化格式无关的 `ReportRecord`、`MessageRenderer` 和默认文本渲染。
4. 把 06-B Runtime 迁移到统一错误别名，验证 `suppressed`、Strong/Weak、表钩子和布尔算术回归不变。
5. 同步更新 DevDocs、UseDocs、crate README 和模块登记；公共 API 文档覆盖率保持 100%。

退出条件：

1. `xiao-diagnostics` 是错误本体的唯一实现，Runtime 不再拥有独立 `RuntimeError` 结构。
2. 可恢复报告和致命报告类型可区分，原因链、上下文、堆栈顺序和 suppressed 错误可稳定测试。
3. 既有 `X06-RUNTIME-*` 编号与 06-B 行为不变，Fatal 使用 `X07-FATAL-*`，不依赖 `xiao-i18n`。
4. Rust 工作区测试、Clippy、Rustdoc、仓库检查和文档覆盖率门禁通过；UseDocs 页面状态为 `verified`。

### H0-B：错误控制流与统一展开消费（07-B）

前置上下文：先阅读 [07-B 交接记录](07-concurrency-and-errors.md#07-b-已完成错误控制流与统一展开消费)、[06A 生命周期静态闭环](06a-lifetime-static-closure.md) 和 [Runtime 错误展开使用文档](../UseDocs/language/memory/runtime/errors-and-unwind.md)。本子阶段只验证 `try`/`catch`/`finally`/`raise` 的语法、静态边界、释放计划和 Runtime 测试驱动器；不启动 VM/LLVM、日志窗口或并发实现。

二级任务：

1. `xiao-syntax` 正例覆盖单个/多个 `catch`、仅 `finally`、`raise` 和文档注释；负例覆盖缺少处理器、缺少 `as`、缺少错误表达式和尾部非法内容。
2. `xiao-types` 覆盖可恢复错误、动态 `raise`、`FatalError` 禁止捕获和具体到宽泛的 `catch` 顺序；断言稳定 `X07-TYPE-*` 编号。
3. `xiao-lifetime` 覆盖 Try/Catch/Finally 隔离作用域、Raise/Catch/UnmatchedError 退出边和每个退出计划的唯一释放动作。
4. `xiao-runtime` 覆盖 `finally -> drop -> catch/传播` 顺序、主错误与 suppressed 合并、未匹配传播、具体类型优先以及 Fatal 独立路由。
5. 更新 UseDocs、crate README、模块登记和 AST/类型/生命周期/Runtime 测试目录 README；公共 API 文档覆盖率保持 100%。

退出条件：

1. Rust workspace 定向测试和完整测试通过，旧 01–07-A 回归不变。
2. `try`/`catch`/`finally`/`raise` 产生统一 AST 和稳定诊断；类型层不执行用户代码。
3. 每条正常、raise、return、循环跳转和未匹配传播边都有可查询释放计划，清理错误不覆盖主错误。
4. `dispatch_catch` 与 `dispatch_fatal` 的结果可审计，Fatal 永不进入普通恢复路径。
5. Clippy `-D warnings`、Rustdoc、仓库检查、文档覆盖率和差分空白检查通过，模块状态才能标记为 `verified`。

退出条件：

1. 前序阶段的词法、类型、容器、模块和生命周期错误使用统一编号、源码位置与传播结构。
2. 字节码和原生后端能够消费同一错误契约，不各自创造错误语义。
3. 数据竞争政策形成编译器与运行时约束，但线程、任务池、`async`/`await` 和 Actor 不作为本阶段实现内容。
4. `try`、`catch`、`finally` 和 `raise` 的可恢复错误传播规则已冻结；`Result` 明确留到后续阶段，`panic` 不可由普通 `catch` 恢复。
5. 错误展开会执行确定性 `drop`，保留主错误和原因链；默认堆栈、标准错误流和结构化日志边界已有规格测试。
6. `[debug]` 的等级、聚焦和诊断事件模型已形成跨后端契约，不能把调试窗口实现成另一套错误语义。

### U0：统一前端集成

U0 拆为 U0-A、U0-B、U0-C 三个二级实现任务；完整交接见 [08A U0 交接记录](08a-u0-frontend-implementation.md)。

#### U0-A：前端流水线装配

1. `FrontendRequest` 能携带源码、项目上下文、规范化配置摘要和可选外部包图。
2. 解析、模块、类型和生命周期阶段按固定顺序执行，诊断累积；存在错误时停止 IR 降低。
3. 前端不执行配置值、模块初始化、表钩子或用户代码。

#### U0-B：类型化 IR 降低

1. `xiao-ir` 使用版本化递归枚举树，覆盖当前已完成的标量、函数、容器、选择器、集合、表、模块、错误控制流和生命周期静态语义。
2. IR 携带类型、控制流、选择器、模块、所有权、释放计划、运行时检查和源码区间；后端不得重新解析或推断。
3. 未知类型只能保守标记为 `dynamic`，不能在 IR 层静默猜测具体类型。

#### U0-C：验证、快照与交接

1. `IrValidator` 检查版本、类型递归结构、集合无索引、控制流后继、所有权/释放计划和错误处理边界。
2. 验证失败返回稳定 `X08-IR-*` 结构化错误并拒绝后端消费。
3. JSON 快照使用显式版本、固定字段顺序和确定性编号；不得写入宿主地址或哈希表迭代顺序。

退出条件：

1. 已完成的词法器、解析器、名称解析、类型检查和逃逸分析通过明确接口串成单一前端流水线。
2. 类型化 IR 完整携带后端所需的类型、控制流、选择器、模块、生命周期与错误信息。
3. 后端不需要重新解析源码或重新推断任何语言语义。
4. U0-A/B/C 定向测试、workspace 回归、Clippy、Rustdoc、仓库检查、文档覆盖率和差异空白检查全部通过。

### 09R1：机型、语义与 ABI 设计

09R 是 09 阶段的前置特别研究工程，完整交接见 [09R. 字节码寄存器机型特别研究](09r-bytecode-machine-research.md)。
`09R*` 与 06-B 已经占用的里程碑代号 `R0` 没有包含或继承关系，也不改变本阶段在里程碑表中的
`B0` 代号。

退出条件：

1. 统一三地址语义模型冻结，且不绑定任何一种物理字节码。
2. 栈式、分类型寄存器式和混合式三种候选机型定义完毕，附结构对比表，且差异存在可被基准区分的量化分界。
3. 分类型寄存器类别、编号空间和动态/对象逃生路径冻结。
4. 函数调用约定、基本块与跳转、异常转移、`finally`/`drop` 清理转移和未捕获错误边界冻结。
5. 指令编码草案、常量池引用方式、源码位置映射和 ABI 版本字段形成设计。
6. 性能门槛与基准协议足以支撑 09R3 的可复现比较。
7. 未决风险逐条登记并给出应对方案；workspace 测试、Clippy、Rustdoc、仓库检查、文档覆盖率和差异空白检查全部通过。

### 09R2：三种机型可运行原型

拆为 09R2a（统一三地址模型、编码器、解释器框架、容器运行时对象与精确索引）和 09R2b
（多选、范围、步长与随机选择全量）。

09R2a 再分批次交付：第一批已落地 TAC 降低、对账验证器、栈式参考解释器、事件接收器与共享
语义向量；第二批已落地容器运行时对象、精确索引路径与临时值释放修复；R2C 已落地错误对象、
handler 路由、finally 子程序、Raise/Check 和异常向量（见 [交接记录](09r-bytecode-machine-research.md)
与 [09R2C 记录](09r2c-exception-control-flow.md)）。寄存器机型、混合式机型和指令编码器
已由 R2D 交付；表声明由 R2H 交付。`for`/迭代已由 R2G 交付，降低器不再把该语句记入
`unsupported`。
这批剩余部分曾由两条记录接手：[09R2D 记录](09r2d-machines-and-encoder.md) 承接机型与编码器，
[09R2b 记录](09r2b-selector-execution.md) 承接选择器全量；两批现均已交付。前者同时登记了 R2C 遗留的缺陷档案、
潜在隐患与排除指南，以及在规划中发现的**跨函数寄存器类别污染**（`TacProgram.categories`
是程序级视图而 `VReg` 编号逐函数重置，导致前序函数的类别被后续函数改写），
后者必须先修，寄存器机型才能建立在其上；R2b 记录只登记与本阶段直接相关的条目并指向前者，
**刻意不复制第二份档案**。

#### 09R2C：异常控制流执行闭环（已完成）

本子阶段把 07-B 的静态错误边界接到 R2a 的 TAC 与栈式 VM：错误类型名单由
`xiao-diagnostics` 单一提供，`RuntimeValue::Error` 保留身份；`TacHandler` 记录受保护区间、
catch 绑定和 finally 子程序；VM 在当前帧查表，未匹配后逐帧展开，Fatal 绕过 catch/finally/释放。
`Check` 已执行 `boolean_condition`、`arithmetic`、`numeric_range` 和窄语义的
`dynamic_conversion`，其他检查明确记入 `TacProgram.unsupported`。

09R2C 退出条件：

1. `r2_stack.rs` 覆盖 catch 绑定重抛、跨帧传播、嵌套循环 finally、嵌套 return 顺序、Check 失败
   稳定码和含堆字符串的 Fatal 隔离。
2. `tests/spec/09-bytecode/errors.json` 覆盖正常/匹配/未匹配/清理/重抛/嵌套以及函数调用、递归、
   循环、raise 和字符串释放的综合路径，并锁定完整释放事件序列。
3. 未消费 RuntimeCheck 不得静默丢失；TAC/VM README、DevDocs、模块登记和差异空白检查同步。
4. 研究 VM 定向测试通过；完整 workspace、Clippy、Rustdoc、Bun 检查和差异空白检查均已通过。

#### 09R2D：两种机型与指令编码器（已完成，2026-09-18）

R2a 的收尾批次，也是 09R3 的入场券——性能门槛要三种机型对比，编码体积是四个门槛指标之一。
范围：修掉逐函数寄存器类别映射的跨函数污染、`Carrier` 接口演进、活跃区间分析、
分类型寄存器机型与混合式机型、`VmMetrics` 机型中立化、三机型共用向量对拍、
指令编码器（含 LEB128 与定宽 `u16` 两种操作数宽度变体）、`pc -> IrSpan` 源码映射与接线。

已交付提交：`64691b6`、`351cb12`、`9b7d887`、`c3bd94c`、`2ac2f2e`、`af779e9`、
`1a2b22f`、`b1bab53`、`1605282`。R2D 基线编码器覆盖 31 个 `TacOp` 变体、ABI 版本头、常量池、
签名表、函数与 handler 元数据、两种操作数宽度，并对坏版本、坏引用、截断、未知
opcode、非法枚举和定宽溢出返回结构化错误。VM 启动时建立只读 pc 表，错误堆栈的
`bytecode_offset` 使用物理 pc；映射缺失保持空后端位置并记录事件。

09R2D 退出条件：

1. 跨函数同编号但不同类别的寄存器不被互相改写；**撤掉修复必须让对应用例失败**。
2. 三种机型跑同一组 `tests/spec/09-bytecode/*.json`，期望值**零改动**，且释放事件序列完全一致。
3. `Poly` 与 `None` 在寄存器机型里有专门路径（落帧槽 / 零宽不占位）。
4. 分配确定（同一输入两次运行分配表逐位相同）；`TAC -> bytes -> TAC` 两种宽度变体均可往返。
5. `bytecode_abi_version` 不匹配时解码必须拒绝，不得静默降级。
6. `BackendLocation.bytecode_offset` 装的是 pc 而不是源码偏移。
7. 研究编码不得使用 `.xiaoc` 扩展名落盘；正式分段格式仍归 14/16 阶段。
8. H1 已改为栈化 `subroutine_faults` 状态；H2 已移除块级指令克隆。交接基准为移除前
   中位数 `253.66 ms`、移除后 `245.83 ms`（同一 R2D 基线向量连续 5 次，约下降
   `3.1%`）；该数字只用于 R3 同口径比较，不是 R3 正式基准结论。

完整任务、硬性约束、上一阶段缺陷档案与排除指南见
[09R2D. 两种机型与指令编码器交接文档](09r2d-machines-and-encoder.md)。

#### 09R2b：选择器全量执行（已完成，2026-09-19）

09R2 的另一半。与 R2D **无依赖**（R2b 是纯语义，与机型无关），但两者都修改了
`xiao-bytecode/src/research/`，因此按既定顺序串行完成。本子阶段已把**冻结并测试过的静态
选择器模型**接到执行层；高级选择不再进入 `unsupported`。表声明构造随后由 R2H 接通，
仍未支持的集合增删和动态方法派发继续由验证器拒绝。

范围：步长接线、`SelectionPlan` 的消费方式、高级选择的 TAC 操作数格式（与精确路径分开）、
Range/All/Step/Random 的运行时执行、`RandomSource` 注入、结果形状构造、
左值广播事务性写入、启用 `selector_bounds`/`selector_step`/`random_count`/`random_seed`
四个运行时检查、语义向量与 UseDocs 措辞更新。

09R2b 退出条件：

1. 多选、闭区间、四种单边范围、全选、正负步长、放回/无放回随机全部可执行，
   且逐条对应 `09-bytecode-runtime.md` 的运行验收。
2. 结果形状保持来源根类型；**字典列重复直接键改判为按选择顺序排列的元组**。
3. 步长按**每个选择项独立**应用；广播写入**先验证全部目标、失败整次回滚**。
4. 随机经 `RandomSource` 注入；同一固定种子可复现；已有实现的冻结行为（种子 0 的常量
   替换、取模采样）不得更改。
5. 四个运行时检查各有区分度用例，且**均由编译期不可知的值触发**（字面量错误会在类型阶段
   被常量折叠拒掉，测不到运行时）。
6. 新增运行时错误码均有产生点，不留死码；UseDocs 中「后续 Runtime 才会…」的措辞已更新。
7. 三种机型复用同一组向量，JSON 零改动；当前共享向量 31 条（新增 `selectors.json` 4 条），
   `r2_stack.rs` 当前 53 条测试。

R2B 新增 `SelectorApply`、`BroadcastAssign`、`RandomSeed` 三个 TAC 操作，编码器由 31 个
扩展到 34 个 opcode；`selector_bounds`、`selector_step`、`random_count`、`random_seed` 四类
RuntimeCheck 已启用，对应 `X06-RUNTIME-017..020`。研究 VM 可执行降低后的程序，但正式
`.xiaoc` 产物和生产 `xiao run` 仍属后续 09/14 阶段。

完整任务、硬性约束与风险见
[09R2b. 选择器全量执行交接文档](09r2b-selector-execution.md)；缺陷档案见
[09R2D 文档](09r2d-machines-and-encoder.md)（R2b 文档刻意不复制第二份）。

#### 09R2b1：选择器执行验证缺口修复（已完成，2026-09-19）

审核发现 R2b 的**核心语义没有验证**：`RunResult::Success` 是无载荷变体（`exec.rs` 里
`Ok(_) => RunResult::Success` 把值丢掉）、`Expectation` 没有结果值字段、新增测试的断言只有
`is_success()` / `error_code()` / `unsupported.is_empty()`。后果是步长被忽略、多选只取首个元素、
结果形状错、字典列重复键没转元组——**全部仍会全绿**。三机型对拍不是安全网：载体只承载值、
不解释指令，选择语义在共享的 `exec.rs` / `ops.rs` 里，算错是三台一起错。

本子阶段补齐选择器结果值的可观察性与值断言，并清理两处一致性问题
（提交信息缺前缀无正文、三处错误码字面量回退）。实现提交为 `2c3a67f` 和 `2ceaf4c`；
新增 `xiao-vm/tests/r2_selector_values.rs`，不改既有 31 条共享 JSON 向量。

09R2b1 退出条件：

1. 选择结果的**值**可被测试断言；每一类选择语义（步长/多选/四种单边范围/全选/形状/
   字典列重复键→元组/嵌套路径/空结果）都有值断言。
2. **撤掉对应实现必须让用例失败**——这是本批存在的唯一理由。本批按**抽样**验证：
   实做两条（步长逐项应用、字典列重复键元组），其余六类由断言里的具体期望值保证，
   未逐条做回退实验。口径与证据对齐的说明见 09R2b1 文档的「验收（完成记录）」。
3. 步长的「逐项应用」与「全局应用一次」有可区分的用例。
4. 错误码字面量清零：`grep -rn 'Some("X0[0-9]-' core/rust/crates/ --include=*.rs` 为 0。

上述退出条件已通过：新增 7 条手工 TAC 夹具在三种载体上断言返回值；步长逐项应用与字典列
重复键元组各完成一次受控回退验证；`cargo test -p xiao-vm --no-fail-fast`、严格 Clippy
和格式检查通过。

完整任务、证据与修法见
[09R2b1. 选择器执行验证缺口修复交接文档](09r2b1-selector-verification.md)。

#### 09R2F1：集合运算执行闭环（已完成，2026-09-20）

`SetOp`/`SetCompare`、集合 RuntimeCheck、三机型共享向量和直接 TAC 夹具已完成；
`sets.json` 共 28 条向量，集合成员检查的声明类型边界经 `Check.expected` 传递。
集合增删和正式生产 Runtime 仍不在本子阶段范围内；表声明由 09R2H 承接。

#### 09R2G：`for` 与迭代执行闭环（已完成，2026-09-20）

本子阶段追加 `TacOp::Len`（opcode 36）与 `TacOp::IndexGetDynamic`（opcode 37），
并完成编码标签、往返校验、引用验证、活跃分析、CFG 和三种载体的统一执行。`for` 降低为
来源求值、长度读取、游标比较、动态索引、绑定和更新桥；`continue` 不跳过游标递增，
空容器零次执行。数组、元组、字符串（Unicode 码点）、集合、字典表和字典列均有执行覆盖，
动态不可迭代值稳定报告 `X06-RUNTIME-024`。

共享 `iteration.json` 新增 13 条向量，另补 1 条集合成员声明类型边界，当时目录总数为 73 条；
`r2_iteration_values.rs` 以手工 TAC 在栈式、分类型寄存器式和混合式载体上直接验证两条
新指令。循环临时来源只在出口释放一次，堆元素按循环体作用域计划逐轮释放，标量循环变量
不会因跨作用域读取被错误提升为堆值。

完整设计、`Check.expected` 的选择理由、区分度验证和门禁证据见
[09R2G 交接文档](09r2g-for-and-iteration.md)。

#### 09R2H：表声明执行闭环（已完成，2026-09-20）

先确定增加程序级 `table_definitions` 区段，再完成表签名镜像、字段辅助函数、静态方法
索引和 opcode 38–40 的八处接线。研究编码布局由 2 升到 3，R3 以版本 3 为冻结起点。
`[T]` 在声明处构造，`new T(...)` 创建独立实例，字段默认值先于 `init` 执行；
运行期复用 06B 状态机，初始化失败回滚、最后强引用析构、弱只读接收者和 Fatal 隔离均已接通。

前端同时补齐已有规则的执行阻断：成员赋值检查、完整方法实参匹配、及时发布已检查成员
接口，以及按方法源码区间读取生命周期参数类型。未扩张表体白名单，也未重算释放计划。
新增 `tables.json` 6 条完整释放序列向量和 13 条三载体专项回归；旧 73 条向量保持不变。
字段写入、析构回调均实际撤回并触发测试失败，恢复后通过。
证据与限制见 [09R2H 交接记录](09r2h-table-declarations.md)。

09R2 阶段退出条件：

1. 三种机型共用同一组语义向量（当前 79 条），且没有为某种机型修改期望结果。
2. 原型只消费已验证 `IrProgram` 或统一三地址结果，不重新解析源码。
3. 最小执行骨架覆盖常量与局部值、算术/比较/显式转换、分支循环调用返回、`try/catch/finally/raise`、作用域释放计划与 `drop`、字符串与表句柄、动态值与运行时错误。
4. TAC 产生的 `(ScopeId, ExitKind)` 释放计划引用保持完整，且实际发出的计划释放序列与冻结计划逐条相等；匿名临时值继续由显式 `Release` 负责。
5. 统一 `VmEventSink` 至少记录模块、函数、作用域、释放、错误、堆栈和运行指标。
6. 研究原型与生产 API 分离；试验性指令和寄存器结构在 09R3 冻结前不作为稳定语言接口暴露。

### 09R3：跨平台基准与冻结

退出条件：

1. 同一源码、同一 IR、同一语义输入下三种机型各自可运行。
2. 基准覆盖标量算术与溢出、布尔奇偶加减、函数调用与递归与分支循环、字符串与表生命周期、`try/finally/drop`、数组/元组/字典/集合、多选与范围与步长与随机选择。
3. 固定 Rust 工具链、优化级别、输入规模、预热次数、重复次数和统计方式；以栈式原型作为同机基线。
4. 语义差分报告、性能报告、内存报告和编码体积报告齐全，并按族分别上报结果。
5. 主候选未通过门槛时必须依据报告选择栈式或混合式，不允许实现代理自行拍板。
6. Windows 复现完成；Linux 与 macOS 记入待复现清单，未复现前不得宣称跨平台已验证。

09R3 结束后冻结：最终字节码机型、寄存器类别与分配策略、函数调用 ABI、异常与清理转移 ABI、
指令编码和版本字段、源码映射格式、性能阈值与基准协议。

#### 09R3 落地记录（2026-09-20）

`tests/benchmarks/Cargo.toml` 提供零框架基准工具：真实 Xiao 源码统一经过
`FrontendCompiler` 和一次 `lower_program`，再由三种载体使用相同实参运行。清单固定 Rust
`1.96.0`、`release`、`opt-level = 3`、`codegen-units = 1`、关闭 LTO、3 次预热、11 次测量、
中位数和四分位区间；编码报告固定 `FORMAT_VERSION = 3`、opcode `0..40`。四个族分别上报，
并逐项确认没有 `string_boolean`、表方法值/动态派发或 `*args`/`**kwargs` 展开实参。

Windows 原生报告已落盘到 `tests/benchmarks/reports/`。语义差分通过；冻结记录的
`family_direction_explanation` 按族记录本次比值和快慢方向，本次样本中四族的寄存器式与混合式
均慢于栈式。两种候选的全局中位数均未达到相对栈式基线至少 10% 的吞吐提升，因此依据报告
选择栈式；这不是实现代理的预先拍板，也没有用全局数字掩盖潜在的族间方向。Linux/macOS 仍
为待复现，WSL/容器数字不进入验收。任何指令集、ABI 或编码改动都使本轮数字作废并要求重新
冻结。

### B0：字节码最小运行闭环

退出条件：

1. 字节码运行接口能通过内部测试驱动器执行经过检查的最小脚本；用户可见的 `xiao run` 在 X0 接入。
2. 字节码调用栈、错误传播和源码回溯可用。
3. 只纳入已经通过前置里程碑的语义。
4. VM 与执行 Runtime 由 Rust 构建；内部驱动器能以结构化结果验证成功、错误、退出码和诊断事件，且不依赖 TypeScript/Node.js 执行用户指令。

### N0：LLVM 原生构建闭环

退出条件：

1. 同一类型化 IR 能通过内部构建驱动器生成 LLVM IR 和原生程序；用户可见的 `xiao build` 在 X0 接入。
2. 原生程序不依赖完整虚拟机。
3. 字节码与原生模式的输出、错误和释放记录一致。

### X0：跨平台工具链

退出条件：

1. CLI 将前序稳定接口接成可用的 `xiao run`、源码快捷运行、`xiao build`、`xiao test` 和 `xiao config`，不复制编译器语义。
2. Windows、Linux、macOS 的最小构建矩阵通过。
3. 从阶段 01 到本阶段已经实现的所有“已确定”规则都有自动化规格测试。
4. CLI 使用 TypeScript 构建并通过静态检查；Windows、Linux、macOS 的命令入口行为一致。
5. 三个平台都提供不要求用户另装 Node.js 的独立 `xiao` 可执行程序，并能正确调用编译器内核。
6. TypeScript CLI 在三平台均能发现并调用兼容版本的 Rust 核心；接口版本失配、核心崩溃和取消都转换为稳定的机器可读诊断，不通过解析本地化文本判断结果。
7. `xiao config CLI.git.summary true` 与 `xiao config --global CLI.git.summary false` 按布尔模式结构化写入目标配置；非法值、未知路径和写入中断具有稳定诊断，且无关配置与注释不被破坏。
8. `-debug` 能从 CLI 传递到运行/构建后端；任何直接运行以及调试产物启动都在用户代码前打开独立诊断窗口，且主程序标准输出和退出码不变。调试版原生程序脱离 `xiao` 启动器、以 GUI 子系统启动时仍须打开窗口；`.xar` 仍由 `xiao -xar` 按 Runtime 前提启动并打开窗口；无法创建窗口时必须报告明确失败，不能静默降级。

### D1：外部包契约与依赖图骨架

退出条件：

1. `config.xiao` 能声明直接外部依赖及版本/来源约束。
2. 依赖解析器能读取本地路径包配置，并在内存中构建完整依赖图。
3. 包身份冲突、缺失依赖和依赖环具有稳定诊断。
4. 本阶段只建立包契约和解析边界，不要求缓存、锁文件或远程下载。

### E0：配置与环境指纹

退出条件：

1. 环境管理器消费第 D0 阶段产出的规范化配置声明树，不执行项目代码。
2. 配置、工具链和目标条件具有稳定指纹，环境元数据为后续锁文件摘要预留字段。
3. `xiao venv` 创建逻辑名 `venv`、目录 `.venv`；`xiao venv dev` 创建逻辑名和目录名都为 `dev`。
4. Shell 钩子能在创建后激活环境，以绿色显示完整 `$环境名$` 前缀，并在切换时恢复原提示符。

### E1：本地依赖与共享缓存

退出条件：

1. 本地路径依赖可以进入经过校验的全局内容寻址缓存。
2. 两个项目环境可以复用相同包内容，同时保持各自的依赖映射。
3. Windows、Linux、macOS 通过只读逻辑映射读取同一缓存内容，不链接或复制依赖源码到项目环境。
4. 项目环境和全局安装环境引用同一缓存；缓存内容使用前经过摘要验证，损坏条目不会进入构建。

### E2：锁定与同步

退出条件：

1. 工具能生成、校验并复用记录完整依赖图的锁文件。
2. `sync` 在目标环境不存在时创建并安装，在已有环境时补齐缺失依赖，成功后通过 Shell 钩子激活该环境。
3. `install` 与 `i` 行为一致：有激活环境时安装到该环境，否则安装到全局环境；二者均不创建或激活环境。
4. 环境映射原子更新；配置与锁文件不一致、缓存损坏和离线缺包具有独立诊断。

### E3：包操作与远程来源

退出条件：

1. 锁定、同步、安装、增加、删除和显式升级分别具有稳定命令与错误边界。
2. 常规运行不会隐式升级依赖，远程内容必须通过完整性校验。
3. 版本求解、离线模式、冻结模式和损坏缓存诊断可用。
4. 一个项目可以使用多个相互独立的包源，不要求单一中央服务器；源适配器对注册表、静态目录、本地目录和符合规范的 GitHub 仓库提供一致的解析结果。
5. 配置源路径后，客户端会读取所有已配置源的最新完整索引快照，并原子地合并出一份联邦源索引；索引记录源身份、配置序号、快照摘要、状态和全部候选版本元数据。
6. 联邦索引保留同名同版本的不同来源，且源快照、索引、包元数据和正文的缓存职责彼此分离；只解析一个依赖时不会下载所有包正文。
7. 显式源优先，未显式指定源时按配置顺序选择第一个可满足约束的源；测试必须证明不会跨源比较版本号择高，无法唯一确定时返回稳定的歧义错误。
8. 有效锁文件和完整缓存命中时可以跳过网络；需要重新解析时各源快照、元数据和正文能够有界并行获取，结果仍可确定性复现。
9. 源不可达时，旧快照可明确标记并按离线规则使用；缺少且可能影响选择的快照时必须报告结果不确定，不能把源当作空源或静默改源。
10. 规范源码包、可选预编译 `.xiaoc` 变体、GitHub 不可变提交/快照和跨源内容摘要校验均通过测试。
11. 首个远程闭环不执行任意安装脚本；包元数据读取、下载和环境物化不会执行包代码。
12. 支持的 Shell 钩子矩阵、取消激活和无钩子降级规则已经冻结并通过跨平台测试。

### I0：终端骨架与单行会话

退出条件：

1. 无参数 `xiao` 启动交互会话，单行模式能够提交、执行、显示错误并继续下一次输入。
2. 版权行、版本行、当前路径、可选环境前缀与 `[X>` 在三个目标平台稳定显示，不支持真彩色的终端具有可读降级。
3. Git 摘要默认关闭；项目或全局配置启用后，分支及领先/落后数量能够刷新且不会阻断提示符。
4. Git 分支没有跟踪上游时的显示规则已经冻结，不依赖 Git 实现的偶然行为。

### I1：多行编辑与运行确认

退出条件：

1. `xiao --inLF` 和会话内 `!inLF!` 都能进入多行模式，`!outLF!` 与 `Shift + Enter` 都能进入运行确认阶段。
2. 多行粘贴保留换行和缩进，并为每个逻辑行分配五字符宽、右对齐的行号和 `|`。
3. 终端软换行只绘制空白行号区与 `|`，不会修改缓冲区、源码位置或下一逻辑行号。
4. 用户确认后执行完整缓冲区，输出区与编辑区由随终端宽度变化的分隔线隔开，并显示耗时与内存摘要。
5. 运行确认状态按 Esc 能无损返回多行编辑，不调用字节码执行入口，也不改变源码缓冲区、源码行号映射或文件绑定。
6. 空缓冲区、99999 行边界、终端尺寸变化和中断信号都有明确行为。
7. 运行确认标题嵌入上方分隔线，下一行只显示独立的 `>` 输入提示符，并随终端宽度稳定缩放。
8. `!outLF!` 可以在触发瞬间显示为带行号输入，但随后从编辑缓冲区和源码行号映射中剔除，不进入词法器或保存内容。
9. 分隔线和逻辑行栏的 `|` 使用 `#7F7F7F`，运行确认输入栏的独立 `>` 使用 `#DCDCAD`；无真彩色终端仍保持可读。

### I2：文件编辑与保存

退出条件：

1. `xiao --inLF "<file.xiao>"` 能把现有 UTF-8 Xiao 源文件装入多行缓冲区，保留换行和缩进供编辑与运行。
2. 未绑定缓冲区使用 `!save!` 或 Ctrl+Shift+S 时进入保存位置输入，首次成功后绑定实体文件并返回编辑。
3. 已绑定缓冲区通过两个保存入口都直接写回，不显示位置输入；`xiao --inLF <file.xiao>` 成功载入时立即建立绑定。
4. 保存提示输入 `q` 并回车或直接按 Esc 都能无损返回编辑，尚未提交的路径输入被丢弃，且不写入文件、不改变文件绑定；首次覆盖和不存在输入文件的待定规则在本里程碑开始前冻结。
5. `!save!` 触发后从可编辑缓冲区、源码行号映射和保存内容中剔除。
6. 写入使用不会留下半文件的安全流程，路径、权限、编码和换行错误具有独立诊断；失败不会破坏已有绑定或错误建立新绑定。
7. 保存状态的上方分隔线显示 `Enter the save location`，路径输入栏的独立 `>` 使用 `#DCDCAD`；光标不属于实际路径文本。

### I3：空命令面板

退出条件：

1. 单行和多行模式都能通过 Ctrl+Shift+P 或 `!panel!` 打开独立命令面板。
2. 多行模式中的 `!panel!` 触发后从可编辑缓冲区、源码行号映射和保存内容中剔除；单行模式中不作为 Xiao 源码执行。
3. 空面板显示嵌入上方分隔线的 `Command Panel`、独立输入栏 `>` 和下方分隔线；线条字符使用 `#7F7F7F`，输入栏角号使用 `#DCDCAD`。
4. 首版不要求任何实际面板命令；按 Esc 或再次按 Ctrl+Shift+P 都能关闭面板并无损返回原交互状态，源码缓冲区和文件绑定保持不变。

### I4：环境包延迟加载

退出条件：

1. 会话根据激活状态只登记全局环境或当前虚拟环境中的可用包，两个环境视图不会交叉泄漏。
2. 启动、提示符渲染、名称枚举和只读接口查询都不执行包模块，首次真正引用内容时才调用运行时加载接口。
3. 包根名称冲突规则已经冻结，不依赖包扫描顺序选择胜者。
4. 模块加载失败具有包名、来源环境和底层原因，失败后会话仍可继续使用。
5. 同一具体模块成功加载后，在一个 REPL 运行实例中重复引用也只初始化一次；首次失败后的重试与缓存行为在本里程碑开始前冻结。

### I5：交互高亮与编辑增强

退出条件：

1. 多行模式复用正式 Token 与源码位置模型提供语法高亮，不另写一套会改变语言语义的解析器。
2. 高亮关闭、终端不支持颜色或源码暂时不完整时仍可编辑、保存和运行。
3. 本里程碑属于后置增强，不阻塞 I0 至 I4 的基础交互闭环。

### L0：国际化与语言包

退出条件：

1. 项目和全局 `config.xiao` 都能解析 `[language].locale`；项目设置覆盖全局设置，未设置时使用内置 `zh-CN`，`zh`/`en` 别名规范化为 `zh-CN`/`en-US`。
2. 编译器、Runtime、CLI、包管理器、REPL 和调试窗口共享同一不可变语言上下文；语言切换不改变源码、错误码、退出码、机器输出或用户程序输出。
3. `XiaoError`、结构化日志和系统提示保留稳定 `code`、`message_id`、参数和实际 `locale`；中文与英语只在展示文本上不同。
4. 缺失翻译按精确标签、基础语言、内置英语目录和原始消息编号/参数稳定回退，格式化失败不会掩盖原始错误。
5. 语言包插件的清单、目录版本、参数签名和 SHA-256 经过校验；首版加载过程不执行插件代码或安装钩子，重复核心消息键会被拒绝。
6. CLI、REPL、错误流、日志和 `-debug` 窗口的中英双语快照及机器输出兼容性测试通过；控制指令和源码位置映射保持不变。
7. 原生调试产物与 `.xar` 在没有项目目录或外部语言插件时仍能使用内置目录显示可读诊断。

### O0：[优化契约与统一管线](13-optimization-contract.md)

退出条件：

1. 优化器、字节码后端和 LLVM 后端共享同一份规范化优化配置与语义约束。
2. 未优化参考路径可稳定生成，且不以任何优化 Pass 作为 B0/N0 的前置条件。
3. IR 验证器能检查类型、控制流、效果、所有权、释放计划和源码映射；验证失败不会继续生成可执行产物。
4. 优化前后差分测试覆盖溢出、`bool +/- 整数`、随机选择、输入输出、动态值、模块加载、容器顺序和 `drop`。
5. 相同输入、工具链、目标和配置产生稳定的优化指纹，临时路径和时间戳不会造成无意义失配。
6. `run`、REPL、普通 `build` 和 `build -xar` 在没有显式级别时均采用 `-O0`；`-O0` 仍执行必要的规范化和验证。

### O1：[字节码优化与 `.xiaoc`](14-bytecode-optimization.md)

退出条件：

1. `.xiaoc` 的单模块边界、“固定文件头 + 分区目录 + 可跳过的可选分区”架构、小端/长度边界规则和无内部压缩规则具有编解码测试；不得以 48 字节文件头或 40 字节目录项作为兼容性前提，分区编号与正文编码在退出本里程碑前冻结。
2. 字节码级 Pass 能逐个启用、验证和回归；窥孔、跳转、常量池和槽布局变换不改变语义。
3. 虚拟机在执行前拒绝损坏、截断、未知版本、错误目标、错误 ABI、栈高度错误和释放计划错误的 `.xiaoc`。
4. 源码、未优化字节码和优化字节码的输出、错误、随机序列、退出码和 `drop` 记录一致。
5. 运行时 quickening/JIT（若未来加入）不会原地改写不可变 `.xiaoc`；其范围和缓存策略已明确记录。
6. 每个 `.xiaoc` 默认含紧凑源码位置映射，不默认含源码正文或完整调试符号；未知必需分区和必需功能位会被拒绝。
7. 字节码执行路径默认生成并缓存经过验证的 `-O0` `.xiaoc`，普通模块和依赖会话状态的 REPL 片段不会错误共享缓存。

### O2：[LLVM 原生优化与链接](15-native-optimization.md)

退出条件：

1. 每个已冻结优化级别都能映射到可记录、可验证的 LLVM Pass 管线，并保留未优化基线。
2. LLVM 不启用破坏 Xiao 溢出、错误、求值顺序、随机、生命周期或 Runtime 检查的变换。
3. 三个平台的原生优化结果与字节码结果通过输出、错误、退出码、容器顺序和释放记录差分。
4. Runtime 裁剪、调试符号、目标 CPU、LTO/PGO 等开关的语义和构建指纹已经冻结，未决定的能力不进入默认级别。
5. 编译时间、启动时间、运行时间、峰值内存和文件体积均有可复现基线与回归阈值。

### O3：[SHA-256 内容寻址与二进制索引](16-content-addressed-artifacts.md)

退出条件：

1. `.xiaoc` 对象对最终规范文件的全部字节计算 SHA-256，以完整 64 字符小写十六进制摘要命名，并能跨平台复现；归档压缩不改变被散列的对象字节。
2. 对象写入、完整校验、原子改名和索引提交在中断、并发和只读场景下不会产生可误用半成品。
3. 每个 `.xar` 只有一个归档 Protobuf 索引，本机缓存另有一个逻辑缓存索引，项目不建立第三个 Protobuf 索引；二者都与依赖锁文件分离。
4. 全局缓存索引损坏时可由已验证对象重建；`.xar` 索引损坏或缺失时必须拒绝归档，不能猜测入口或资源映射。
5. 缓存复用不会跨越语言/IR/VM/Runtime 版本、目标平台、依赖锁或优化配置错误命中。

### O4：[`.xar` 字节码归档与启动](17-xar-archive.md)

退出条件：

1. `.xar` 使用受限 ZIP/ZIP64，固定且仅有一个 `META-INF/xiao/index.pb`；内容寻址成员布局、压缩选择、版本和完整性规则具有格式测试。
2. 归档创建、列目录、验证和运行均拒绝路径穿越、重复索引/成员、越界长度、ZIP bomb、加密、多卷、缺失对象和版本不兼容。
3. 命令行入口与操作系统双击入口调用同一 `xiao` 字节码运行接口，入口、输出、错误和退出码一致。
4. 归档包含锁定的外部/第三方依赖，不包含由 Xiao Runtime 提供的官方依赖；资源、源码和调试信息的取舍已经明确，不能把外部环境假设写进未声明的清单。
5. Windows、Linux、macOS 的文件关联、工作目录、参数、权限和无关联退化路径均通过测试；没有安装 `xiao` 时不得声称 `.xar` 可独立运行。
6. `config.xiao`、含 `[main]` 的 `.xiao` 和无项目表的独立 `.xiao`（包括单条 `print`）都能作为合法打包输入。
7. 标准归档只包含显式声明的资源，默认保留紧凑源码位置映射而不包含源码正文和完整调试符号；ZIP 成员元数据和顺序可复现。
8. 通用 ZIP 工具只需能够列出或解压物理成员；入口、依赖、模块语义和 Protobuf 字段由 `xiao`/Xiao Runtime 解释，归档不要求携带 descriptor 或自包含 Runtime。

### O5：[优化与产物 CLI 接入](18-optimization-cli.md)

退出条件：

1. `xiao build -xar "<config.xiao|main.xiao|任意.xiao>" [-O0|-O1|-O2|-O3]` 与 `xiao -xar "<file.xar>"` 的语法已经确定；其参数诊断、错误码和帮助文本在退出本里程碑前冻结，其余辅助命令另行决定。
2. CLI 能将 `config.xiao`、全局配置和命令行覆盖归一化为同一后端配置，不在 TypeScript 中复制编译或格式语义。
3. `.xiao`、`.xiaoc` 和 `.xar` 的输入路由、缓存命中/失配、版本拒绝和失败回退行为可观察且可脚本化。
4. 三平台独立 CLI 能安装/检查/移除 `.xar` 文件关联，且不要求用户另装 Node.js。
5. `xiao build -xar "<源路径>" [-O0|-O1|-O2|-O3]` 生成可验证归档，`xiao -xar "<归档路径>"` 通过同一运行入口执行；不存在 Xiao Runtime 时给出明确失败诊断。
6. 不写 `-O` 且配置未指定级别时统一采用 `-O0`；字节码路径默认缓存 `.xiaoc`，普通源码目录不会出现隐式缓存文件。
7. `-debug` 与 `[debug]` 配置能传递到运行和构建后端；终端等级、文件等级、堆栈、默认分流和 `mirror` 聚焦规则可观察，且不改变程序标准输出或退出语义。调试产物保存强制激活位，直接启动时必定打开独立诊断窗口；调试版原生程序不依赖 `xiao` 启动器，`.xar` 仍按 Runtime 前提由 `xiao -xar` 启动。

### O6：[优化、兼容性与发布验收](19-optimization-release.md)

退出条件：

1. 已冻结优化级别、字节码格式、索引 Schema、`.xar` 版本和目标平台具有完整兼容矩阵与升级/拒绝策略。
2. 优化前后、不同后端、不同归档入口的差分、模糊、损坏恢复和安全测试全部通过。
3. SHA-256 完整性验证、资源上限、路径安全、权限和离线缓存行为在三平台一致；签名/信任未实现时有明确提示。
4. 发布报告可追溯源码、依赖锁、工具链、优化指纹、对象摘要和归档成员，且重复构建差异在白名单内。
5. `xiao build` 的 LLVM 原生发布构建完成相对于已冻结 Java 基线的性能对照，并按 Windows → Linux → macOS 记录结果；Rust 字节码 VM 只保留独立性能报告。基准、版本或阈值未冻结时不得宣称“至少达到 Java”。

## 一级工程目标：维护文档与决策

### 变更流程

1. 新规则先写入 `00-decisions.md`。
2. 再更新受影响阶段的语法、IR、运行时和测试章节。
3. 实现与文档冲突时，先标记待定并补充回归测试。
4. 每个里程碑完成后在 README 的状态表更新状态。

### 禁止事项

- 不把实验性语法直接写成稳定语法。
- 不让字节码后端和 LLVM 后端各自发明类型或释放规则。
- 不用“测试没覆盖”替代未解决的语言设计决策。

## 二级实现任务

### 测试基础设施

1. 建立源码快照、AST 快照和 IR 快照测试。
2. 建立正例/负例目录和统一期望文件格式。
3. 建立字节码/原生差分执行器。
4. 建立跨平台 CI 和最小发布样例。
5. 建立 IR/字节码/索引/归档的规范化快照、摘要重现和损坏样本库。
6. 建立优化 Pass 单独开关、性能/体积基准和可复现构建比较器。

### 质量门槛

1. 每次语法改动必须更新规格测试。
2. 每次 Runtime 改动必须运行生命周期和内存错误测试。
3. 每次 LLVM 改动必须运行双模式差分测试。
4. 每次配置改动必须验证命令行覆盖和原生固化行为。
5. 每次优化 Pass、字节码格式、索引或归档改动必须运行对应的语义、完整性和兼容性测试。
6. 每次消息目录、语言配置或语言包插件改动必须同时运行中英双语展示测试和机器可读错误/日志兼容性测试。

## 验收标准

### 发布验收

- 文档索引顺序与实际开发依赖一致。
- 每个阶段至少有两个层级的任务和可执行退出条件。
- 所有未决问题在对应阶段开始前有明确结论或被主动延期。
