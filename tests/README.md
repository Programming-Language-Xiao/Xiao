# `tests/`

## 目录职责

存放所有可重复的规格、单元、集成、差分、性能和模糊测试。测试辅助函数也属于项目代码，必须遵守全仓库文档注释门槛。

## 工程期

从 F0 开始持续建设；每个语言或 Runtime 阶段同步增加测试，不能把测试推迟到发布期。

## 子目录

见 `spec`、`unit`、`integration`、`differential`、`benchmarks`、`fuzz` 和 `fixtures` 的 README。
